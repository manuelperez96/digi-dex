/**
 * @description A short version of data about a concrect Digimon
 * @class
 */
export default class PageableDigimonOverview {
  /**
   * @description Return the information about a list of digimon and the current
   * page in the api
   * @param {object} json - Have to be a json object, if not throw an exception
   */
  constructor(json) {}
}

/**
 * @description A short version of data about a concrect Digimon
 * @class
 */
export default class DigimonOverview {
  /**
   * @description Creates a new Digimon instance using a Json map.
   * @param {object} json - Have to be a json object, if not throw an exception
   */
  constructor(json) {}
}
